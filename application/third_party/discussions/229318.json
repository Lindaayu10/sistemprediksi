[
  {
    "Id": "501099",
    "ThreadId": "229318",
    "Html": "<p>Hi , I've got next issue,</p>\r\n<p>I use template line &nbsp;with placeholders</p>\r\n<p>%%name%%&nbsp; %%income%% etc</p>\r\n<p>I add pack of new lines below it</p>\r\n<p>$objWorksheet-&gt;insertNewRowBefore($cells_info[&quot;row&quot;]+1, count($this -&gt; data['affiliates_overview']));<br><br>then I put data to each line</p>\r\n<p>foreach ($this -&gt; data['affiliates_overview'] as $row) { //get row&nbsp;<br> $row_index++;<br>&nbsp;foreach ($row as $name =&gt; $value) { //get cell<br> if (!empty($cells_info['cells'][$name])){ <br> $cell_index = $cells_info['cells'][$name]['column'].$row_index;<br>&nbsp; if ($name == 'name' &amp;&amp; $value != 'TOTAL') { //add name with link<br>&nbsp; &nbsp; $objWorksheet-&gt;getCell($cell_index)-&gt;setValue(preg_replace(&quot;/%%.+%%/&quot;, $value, $cells_info['cells'][$name]['placeholder']));<br>&nbsp; &nbsp; &nbsp;$objWorksheet-&gt;getCell($cell_index)-&gt;getHyperlink()-&gt;setUrl(&quot;sheet://'&quot;.substr($row-&gt;affiliate_.&quot; &quot;. $row-&gt;name, 0, XML_MAX_TITLE_LENGTH).&quot;'!A1&quot;);<br>&nbsp; &nbsp; $objWorksheet-&gt;getStyle($cell_index)-&gt;getFont()-&gt;setUnderline(true);<br>&nbsp; &nbsp; $objWorksheet-&gt;getStyle($cell_index)-&gt;getFont()-&gt;getColor()-&gt;setARGB(PHPExcel_Style_Color::COLOR_BLUE);<br>&nbsp; } else { &nbsp; // add any value<br>&nbsp; &nbsp; $objWorksheet-&gt;setCellValue($cell_index, preg_replace(&quot;/%%.+%%/&quot;, $value, $cells_info['cells'][$name]['placeholder']));<br>&nbsp; }<br> }<br> }<br> }</p>\r\n<p>do dump and can see links in each line ,&nbsp; then I remove placeholder line</p>\r\n<p>$objWorksheet-&gt;removeRow($cells_info['row'],1);</p>\r\n<p>after this I do dump and there is link only in first line, but if I hide placeholder line</p>\r\n<p>&nbsp;$objWorksheet-&gt;getRowDimension($cells_info['row'])-&gt;setVisible(false);</p>\r\n<p>links don't disappear</p>\r\n<p>Is it bug or I do something wrong ? Also&nbsp;getFont()-&gt;setUnderline(true) doesn't work too, I can't understand why.</p>",
    "PostedDate": "2010-10-01T08:05:26.33-07:00",
    "UserRole": null,
    "MarkedAsAnswerDate": null
  }
]